image:
  tag: "latest"

job:
  enabled: true
  image:
    tag: "latest"

ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
  hosts:
    - paths:
        - path: "/team-service/(?!.*/private/).*"
          pathType: ImplementationSpecific
  tls: []